<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Logging Configuration
    |--------------------------------------------------------------------------
    */
    'log_failures' => false, // Отключаем логи (или true, если нужно отслеживать)

    /*
    |--------------------------------------------------------------------------
    | Include Currency in Results
    |--------------------------------------------------------------------------
    */
    'include_currency' => false, // Не нужно для РФ

    /*
    |--------------------------------------------------------------------------
    | Default Service
    |--------------------------------------------------------------------------
    */
    'service' => 'maxmind_database', // 🔴 Указываем драйвер по умолчанию

    /*
    |--------------------------------------------------------------------------
    | Storage Specific Configuration
    |--------------------------------------------------------------------------
    */
    'services' => [

        'maxmind_database' => [
            'class' => \Torann\GeoIP\Services\MaxMindDatabase::class,
            'database_path' => storage_path('app/geoip.mmdb'), // 🟢 Путь к твоему файлу
            'update_url' => '', // ⚠️ Пусто — обновления не нужны
            'locales' => ['ru', 'en'], // 🔤 Приоритет: русский
        ],

        // Все API-сервисы можно отключить или закомментировать
        // Чтобы не было попыток выхода в интернет

    ],

    /*
    |--------------------------------------------------------------------------
    | Default Cache Driver
    |--------------------------------------------------------------------------
    */
    //'cache' => 'all', // Кэшируем результаты
    'cache' => true, // Включаем кэш, но без тегов

    /*
    |--------------------------------------------------------------------------
    | Cache Tags
    |--------------------------------------------------------------------------
    */
    //'cache_tags' => ['torann-geoip-location'],
    'cache_tags' => [], // Отключаем теги

    /*
    |--------------------------------------------------------------------------
    | Cache Expiration (в минутах)
    |--------------------------------------------------------------------------
    */
    'cache_expires' => 1440, // 1 день — база не меняется часто

    /*
    |--------------------------------------------------------------------------
    | Default Location (fallback)
    |--------------------------------------------------------------------------
    */
    'default_location' => [
        'ip' => '127.0.0.1',
        'iso_code' => 'RU',
        'country' => 'Russia',
        'city' => 'Москва', // 🔴 Теперь по умолчанию — Москва
        'state' => 'MOW',
        'state_name' => 'Москва',
        'postal_code' => null,
        'lat' => 55.7558,
        'lon' => 37.6173,
        'timezone' => 'Europe/Moscow',
        'continent' => 'EU',
        'default' => true,
        'currency' => 'RUB',
    ],

];
